rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public collaborative access for SAR Correspondence Register
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
